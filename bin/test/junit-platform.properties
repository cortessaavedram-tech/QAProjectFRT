# Indicar a JUnit que use Cucumber
cucumber.publish.quiet=true

# Definir todos los plugins en una sola línea
# Cambiamos la ruta a build/cucumber-report.html para evitar colisiones
cucumber.plugin=pretty, html:build/reports/cucumber/report.html, io.qase.cucumber7.QaseEventListener
qase.enable=true
qase.mode=testops